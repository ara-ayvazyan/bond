syntax = "proto2";
package example;

import "folder/other.proto";

message Example
{
    optional int32 i = 1 [default=123];
    optional string str = 2;
    optional other.Other msg = 3;
    optional other.Other lazy_msg = 4 [lazy=true];
    optional fixed64 x = 5;
    map<int32, float> m = 6;
    repeated uint64 integers = 7;
    repeated string strs = 8;
    optional other.MyEnum e = 9 [default=Value3];
}
